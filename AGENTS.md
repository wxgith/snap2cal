# Snap2Cal 长期开发约束

1. 核心解析逻辑不得依赖 React。
2. 不得编造缺失的日期、时间或地点。
3. 所有推断必须生成用户可见的提示。
4. 相对日期解析必须接收外部参考时间。
5. 用户手工修改不得被静默覆盖。
6. 不得引入或调用外部 API。
7. 不得上传用户数据；输入只在浏览器内存中处理。
8. 每次行为变更必须增加或修改测试。
9. OCR 范围限于第二阶段单张图片本地识别，不得扩展为批量或云端识别。
10. 完成任务前必须运行测试、类型检查和代码检查。
11. 图片和 OCR 文本不得上传或持久化。
12. OCR 必须通过 `OcrAdapter` 接入，React 组件不得直接依赖具体 OCR 引擎。
13. 现有文本解析器不得依赖图片坐标；`SourceSpan` 与图片区域通过独立映射层关联。
14. OCR `originalText` 不得被用户编辑覆盖。
15. OCR 运行时不得回退到第三方 CDN，资源必须来自应用自身域名。
16. 用户更换或删除图片时必须取消旧任务并防止旧结果覆盖新状态。
17. 所有 Object URL 和 OCR Worker 必须释放。
18. OCR 自动化测试优先使用确定性 Mock；真实 OCR 结果不得用 Mock 冒充。
19. 第一阶段纯文本功能不得回归。
20. `parseEventText` 必须保持单事件返回类型；多事件检测通过独立模块实现。
21. 多事件模块不得复制单事件日期、时间、地点或标题解析规则。
22. 所有候选必须保留指向原始输入的 `SourceSpan`，规范化文本必须能映射回原文。
23. `SourceSpan` 继续使用 JavaScript UTF-16 半开区间，不得通过搜索重复文字恢复位置。
24. 共享日期和地点必须保留真实来源，且不得覆盖候选自身的显式字段。
25. 候选置信度、字段置信度和 OCR 置信度必须彼此区分。
26. 低置信度或无效候选不得静默导出；重复候选不得自动删除。
27. 重新检测只可向稳定 ID、相同区间和相同原文的候选迁移手工修改。
28. 多事件 ICS 必须复用现有单个 `VEVENT` 生成逻辑，并保持单事件兼容。
29. 课程表和排班表的二维结构识别不属于第三阶段范围。
30. 所有新增多事件行为必须具有确定性单元、组件或 E2E 测试。
31. 课程表必须使用独立二维模型，不得交给线性 `CandidateSegmenter`。
32. 课程表网格线必须保存规范化图片自然坐标，CSS 像素不得写入领域模型。
33. OCR 块到课程表单元格的分配必须基于中心点、面积重叠和明确歧义提示。
34. 用户编辑课程单元格只能修改 `text`，不得覆盖 `originalText` 或回写 `OcrBlock`。
35. 自动检测课程表网格后必须允许人工移动、新增、删除、锁定和恢复网格线。
36. 低置信度网格不得静默生成课程，必须经过用户确认。
37. 星期列映射必须唯一；重复星期或未映射课程列必须阻止课程生成。
38. 节次标签不得用于推断实际作息时间，缺少起止时间必须阻止对应 occurrence。
39. 不得硬编码任何学校的作息时间、教学周日期或校历例外。
40. 第一教学周星期一必须由用户明确输入，课程日期不得读取运行时当前日期。
41. 所有课程周次必须规范化为明确、升序且去重的 `weeks` 数组。
42. 课程单元格第一版只允许同一星期列内的连续纵向合并，不支持横跨星期。
43. 网格、表头、时间或周次变化后必须使不再适用的模板和 occurrence 状态失效。
44. occurrence 排除不得删除原始课程模板；冲突只警告，不得自动删除事件。
45. 每次课程 occurrence 必须导出为独立 `VEVENT`，本阶段不得生成 `RRULE`。
46. 课程表 ICS 必须复用现有 `EventDraft`、单个 `VEVENT` 和批量日历生成逻辑。
47. 课程表模式不得清除文字模式或普通图片模式状态，课程表模块必须延迟加载。
48. 课程表图片、OCR、网格、课程和排除状态不得上传或持久化。
49. 课程表 E2E 使用确定性 Mock OCR 与 Mock GridDetector；真实网格算法另设浏览器冒烟。
50. 排班表不属于第四阶段，不得复用“星期列 + 节次行”模型强行实现。
51. 所有课程表算法必须具有纯函数或确定性测试，完成前运行 `verify:schedule-table`。
52. 排班表使用独立 `shift-roster` 语义层。
53. 不得用课程表 `CourseTemplate` 表示排班。
54. 底层网格算法只能维护一份。
55. 所有网格和单元格坐标使用图片自然坐标。
56. 日期表头不得静默使用当前年份或月份。
57. 日号解析必须依赖用户确认的年月。
58. 人员姓名不得用于推断敏感属性。
59. 班次代码只能进行精确映射。
60. 不得用模糊匹配自动分配班次。
61. 非空代码不得自动视为空白。
62. 班次定义必须由用户确认。
63. 跨午夜必须由用户明确确认。
64. occurrence 日期不得依赖当前日期。
65. 跨夜日期必须使用日历日期运算。
66. 不得通过固定毫秒增加一天。
67. skip assignment 不生成事件但不得删除。
68. 单次排除不得删除原 assignment。
69. 冲突只提示，不自动删除。
70. 不得提供劳动法规或工资结论。
71. 不得生成 `RRULE`。
72. 不得实现换班和审批。
73. 个人和团队 ICS 必须复用现有 `VEVENT` 逻辑。
74. 修改网格必须使下游语义状态失效。
75. 生产包不得包含 Mock Roster、Mock OCR 或 Mock GridDetector。
76. 排班表不得影响课程表和现有事件模式。
77. 所有新增排班行为必须具有确定性测试。
